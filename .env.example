PORT=
JWT_SECRET=
JWT_EXPIRES_IN=
BASE_URL=
FRONTEND_URL=
MONGODB_URI=
RESEND_API_KEY=
RESET_PASSWORD_EXPIRES_IN=
GOOGLE_CLIENT_ID=
NEXT_PUBLIC_GOOGLE_CLIENT_ID=
EMAIL_FROM=
SENTRY_DSN=
NEXT_PUBLIC_SENTRY_DSN=
SENTRY_AUTH_TOKEN=
LOG_LEVEL=
CRON_SECRET=
STRIPE_SECRET_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_PRICE_MONTHLY=
STRIPE_PRICE_ANNUAL=
NEXT_PUBLIC_STRIPE_ENABLED=
INVITE_ONLY=            # 'true' to gate registration; unset/false = open
INVITE_CODES=           # comma-separated reusable codes (server enforcement)
NEXT_PUBLIC_INVITE_ONLY= # 'true' to show the invite field in the register UI
BOTID_ENABLED=
NEXT_PUBLIC_BOTID_ENABLED=
DPO_CONTACT=            # LGPD data-protection contact shown by GET /api/auth/me/access; falls back to a placeholder
TRUSTED_PROXY=          # 'true' if a non-Vercel deploy sits behind a reverse proxy that overwrites X-Forwarded-For; unset off-Vercel makes rate limiting IP-blind (fail-open) and is reported at boot
